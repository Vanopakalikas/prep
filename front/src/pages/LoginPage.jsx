import { useLoaderData } from "react-router-dom"

export default function ItemPage(){
    return(
        <main className = "w-full h-full flex items-center justify-center">
            <div className="w-40 h-40">  sw </div>
        </main>
    )
}